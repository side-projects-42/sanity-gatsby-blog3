var __defProp = Object.defineProperty;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, {enumerable: true, configurable: true, writable: true, value}) : obj[key] = value;
var __assign = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __rest = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
import React, {forwardRef} from "react";
import styled from "styled-components";
import {EMPTY_RECORD} from "../../constants";
import {LayerProvider} from "./layerProvider";
import {useLayer} from "./useLayer";
const Root = styled.div`
  position: relative;
`;
const LayerChildren = forwardRef(function LayerChildren2(props, ref) {
  const {children, style = EMPTY_RECORD} = props, restProps = __rest(props, ["children", "style"]);
  const {zIndex} = useLayer();
  return /* @__PURE__ */ React.createElement(Root, __assign(__assign({}, restProps), {
    ref,
    style: __assign(__assign({}, style), {zIndex})
  }), children);
});
const Layer = forwardRef(function Layer2(props, ref) {
  const {children, zOffset = 1} = props, restProps = __rest(props, ["children", "zOffset"]);
  return /* @__PURE__ */ React.createElement(LayerProvider, {
    zOffset
  }, /* @__PURE__ */ React.createElement(LayerChildren, __assign(__assign({}, restProps), {
    ref
  }), children));
});
export {
  Layer
};
