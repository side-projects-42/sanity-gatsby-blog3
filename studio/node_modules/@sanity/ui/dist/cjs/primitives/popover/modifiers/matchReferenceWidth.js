var __defProp = Object.defineProperty;
var __markAsModule = (target) => __defProp(target, "__esModule", {value: true});
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, {get: all[name], enumerable: true});
};
__markAsModule(exports);
__export(exports, {
  matchReferenceWidthModifier: () => matchReferenceWidthModifier
});
const matchReferenceWidthModifier = {
  name: "matchWidth",
  enabled: true,
  phase: "beforeWrite",
  requires: ["computeStyles"],
  fn({state}) {
    const {width} = state.rects.reference;
    state.styles.popper.width = `${width}px`;
  },
  effect: ({state}) => {
    const refElement = state.elements.reference;
    if (refElement instanceof HTMLElement) {
      state.elements.popper.style.width = `${refElement.offsetWidth}px`;
    }
  }
};
