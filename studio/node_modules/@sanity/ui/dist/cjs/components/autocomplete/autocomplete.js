var __create = Object.create;
var __defProp = Object.defineProperty;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, {enumerable: true, configurable: true, writable: true, value}) : obj[key] = value;
var __assign = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __markAsModule = (target) => __defProp(target, "__esModule", {value: true});
var __rest = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, {get: all[name], enumerable: true});
};
var __exportStar = (target, module2, desc) => {
  if (module2 && typeof module2 === "object" || typeof module2 === "function") {
    for (let key of __getOwnPropNames(module2))
      if (!__hasOwnProp.call(target, key) && key !== "default")
        __defProp(target, key, {get: () => module2[key], enumerable: !(desc = __getOwnPropDesc(module2, key)) || desc.enumerable});
  }
  return target;
};
var __toModule = (module2) => {
  return __exportStar(__markAsModule(__defProp(module2 != null ? __create(__getProtoOf(module2)) : {}, "default", module2 && module2.__esModule && "default" in module2 ? {get: () => module2.default, enumerable: true} : {value: module2, enumerable: true})), module2);
};
__markAsModule(exports);
__export(exports, {
  Autocomplete: () => Autocomplete
});
var import_icons = __toModule(require("@sanity/icons"));
var import_react = __toModule(require("react"));
var import_constants = __toModule(require("../../constants"));
var import_helpers = __toModule(require("../../helpers"));
var import_hooks = __toModule(require("../../hooks"));
var import_primitives = __toModule(require("../../primitives"));
var import_autocompleteOption = __toModule(require("./autocompleteOption"));
var import_styles = __toModule(require("./styles"));
const EMPTY_RECORD = {};
const defaultRenderValue = (value, option) => option ? option.value : value;
const defaultFilterOption = (query, option) => option.value.toLowerCase().indexOf(query.toLowerCase()) > -1;
const LIST_IGNORE_KEYS = [
  "Control",
  "Shift",
  "Alt",
  "Enter",
  "Home",
  "End",
  "PageUp",
  "PageDown",
  "Meta",
  "Tab",
  "CapsLock"
];
const InnerAutocomplete = (0, import_react.forwardRef)((props, ref) => {
  const {
    border = true,
    filterOption: filterOptionProp,
    fontSize = 2,
    icon,
    id,
    loading,
    onChange,
    onQueryChange,
    onSelect,
    openButton,
    options: optionsProp,
    padding: paddingProp = 3,
    radius = 2,
    renderOption: renderOptionProp,
    renderValue = defaultRenderValue,
    value: valueProp = ""
  } = props, restProps = __rest(props, [
    "border",
    "filterOption",
    "fontSize",
    "icon",
    "id",
    "loading",
    "onChange",
    "onQueryChange",
    "onSelect",
    "openButton",
    "options",
    "padding",
    "radius",
    "renderOption",
    "renderValue",
    "value"
  ]);
  const defaultRenderOption = (0, import_react.useCallback)(({value: value2}) => /* @__PURE__ */ import_react.default.createElement(import_primitives.Card, {
    as: "button",
    padding: paddingProp,
    tone: "inherit"
  }, /* @__PURE__ */ import_react.default.createElement(import_primitives.Text, {
    size: fontSize
  }, value2)), [fontSize, paddingProp]);
  const renderOption = typeof renderOptionProp === "function" ? renderOptionProp : defaultRenderOption;
  const filterOption = typeof filterOptionProp === "function" ? filterOptionProp : defaultFilterOption;
  const [value, setValue] = (0, import_react.useState)(valueProp);
  const [query, setQuery] = (0, import_react.useState)(null);
  const valueRef = (0, import_react.useRef)(value);
  const [focused, setFocused] = (0, import_react.useState)(false);
  const listboxId = `${id}-listbox`;
  const options = Array.isArray(optionsProp) ? optionsProp : import_constants.EMPTY_ARRAY;
  const [selectedIndex, setSelectedIndex] = (0, import_react.useState)(-1);
  const inputRef = (0, import_react.useRef)(null);
  const listRef = (0, import_react.useRef)(null);
  const activeItemId = selectedIndex > -1 ? `${id}-option-${selectedIndex}` : void 0;
  const padding = (0, import_hooks.useResponsiveProp)(paddingProp);
  const rootRef = (0, import_react.useRef)(null);
  const currentOption = value ? options.find((o) => o.value === value) : void 0;
  const filteredOptions = (0, import_react.useMemo)(() => options.filter((option) => query ? filterOption(query, option) : true), [filterOption, options, query]);
  const optionsLen = filteredOptions.length;
  const expanded = loading || focused && optionsLen > 0 && query !== null;
  const forwardedRef = (0, import_hooks.useForwardedRef)(ref);
  const handleRootBlur = (0, import_react.useCallback)(() => {
    setTimeout(() => {
      const rootEl = rootRef.current;
      const focusedEl = document.activeElement;
      const focusInside = rootEl && focusedEl && rootEl.contains(focusedEl);
      if (!focusInside)
        setFocused(false);
    }, 0);
  }, []);
  const handleRootKeyDown = (0, import_react.useCallback)((event) => {
    var _a, _b;
    if (event.key === "ArrowDown") {
      event.preventDefault();
      if (!optionsLen)
        return;
      setSelectedIndex((index) => (index + 1) % optionsLen);
      return;
    }
    if (event.key === "ArrowUp") {
      event.preventDefault();
      if (!optionsLen)
        return;
      setSelectedIndex((index) => {
        return index === -1 ? optionsLen - 1 : (optionsLen + index - 1) % optionsLen;
      });
      return;
    }
    if (event.key === "Escape") {
      setFocused(false);
      valueRef.current = "";
      setQuery(null);
      if (onQueryChange)
        onQueryChange(null);
      (_a = inputRef.current) == null ? void 0 : _a.focus();
      return;
    }
    const target = event.target;
    const listEl = listRef.current;
    if ((listEl === target || (listEl == null ? void 0 : listEl.contains(target))) && !LIST_IGNORE_KEYS.includes(event.key)) {
      (_b = inputRef.current) == null ? void 0 : _b.focus();
      return;
    }
  }, [onQueryChange, optionsLen]);
  const handleInputChange = (0, import_react.useCallback)((event) => {
    const q = event.currentTarget.value;
    valueRef.current = q;
    setQuery(q);
    if (onQueryChange)
      onQueryChange(q);
  }, [onQueryChange]);
  const handleInputFocus = (0, import_react.useCallback)(() => setFocused(true), []);
  const handleClearButtonClick = (0, import_react.useCallback)(() => {
    var _a;
    valueRef.current = "";
    setValue("");
    if (onChange)
      onChange("");
    setQuery(null);
    if (onQueryChange)
      onQueryChange(null);
    (_a = inputRef.current) == null ? void 0 : _a.focus();
  }, [onChange, onQueryChange]);
  const handleClearButtonFocus = (0, import_react.useCallback)(() => setFocused(true), []);
  const handleOptionSelect = (0, import_react.useCallback)((v) => {
    var _a;
    if (onSelect)
      onSelect(v);
    setValue(v);
    if (onChange)
      onChange(v);
    setQuery(null);
    if (onQueryChange)
      onQueryChange(null);
    setFocused(false);
    (_a = inputRef.current) == null ? void 0 : _a.focus();
  }, [onChange, onSelect, onQueryChange]);
  const handleOpenClick = (0, import_react.useCallback)(() => {
    var _a;
    (_a = inputRef.current) == null ? void 0 : _a.focus();
    setQuery(query || "");
  }, [query]);
  (0, import_react.useEffect)(() => {
    if (valueProp !== valueRef.current) {
      valueRef.current = valueProp;
      setValue(valueProp);
    }
  }, [valueProp]);
  (0, import_react.useEffect)(() => setSelectedIndex(-1), [optionsLen]);
  (0, import_react.useEffect)(() => {
    const listElement = listRef.current;
    if (!listElement)
      return;
    const selectedItemElement = listElement.childNodes[selectedIndex];
    if (selectedItemElement) {
      (0, import_helpers.focusFirstDescendant)(selectedItemElement);
    }
  }, [selectedIndex]);
  const setRef = (el) => {
    inputRef.current = el;
    forwardedRef.current = el;
  };
  const clearButton = (0, import_react.useMemo)(() => value.length > 0 ? {
    "aria-label": "Clear",
    onFocus: handleClearButtonFocus
  } : void 0, [handleClearButtonFocus, value]);
  const openButtonBoxPadding = (0, import_react.useMemo)(() => padding.map((v) => v - 2), [padding]);
  const openButtonPadding = (0, import_react.useMemo)(() => padding.map((v) => v - 1), [padding]);
  const openButtonProps = (0, import_react.useMemo)(() => typeof openButton === "object" ? openButton : EMPTY_RECORD, [openButton]);
  const openButtonNode = (0, import_react.useMemo)(() => openButton ? /* @__PURE__ */ import_react.default.createElement(import_primitives.Box, {
    padding: openButtonBoxPadding
  }, /* @__PURE__ */ import_react.default.createElement(import_primitives.Button, __assign(__assign({}, openButtonProps), {
    fontSize,
    icon: import_icons.ChevronDownIcon,
    mode: "bleed",
    onClick: handleOpenClick,
    padding: openButtonPadding
  }))) : void 0, [
    fontSize,
    handleOpenClick,
    openButton,
    openButtonBoxPadding,
    openButtonPadding,
    openButtonProps
  ]);
  return /* @__PURE__ */ import_react.default.createElement(import_styles.Root, {
    "data-ui": "Autocomplete",
    onBlur: handleRootBlur,
    onKeyDown: handleRootKeyDown,
    ref: rootRef
  }, /* @__PURE__ */ import_react.default.createElement(import_primitives.TextInput, __assign(__assign({}, restProps), {
    "aria-activedescendant": activeItemId,
    "aria-autocomplete": "list",
    "aria-expanded": expanded,
    "aria-owns": listboxId,
    autoCapitalize: "off",
    autoComplete: "off",
    autoCorrect: "off",
    border,
    clearButton,
    fontSize,
    icon,
    id,
    inputMode: "search",
    onChange: handleInputChange,
    onClear: handleClearButtonClick,
    onFocus: handleInputFocus,
    padding,
    radius,
    ref: setRef,
    role: "combobox",
    spellCheck: false,
    suffix: openButtonNode,
    value: query === null ? renderValue(value, currentOption) : query
  })), /* @__PURE__ */ import_react.default.createElement(import_styles.ListBoxContainer, {
    hidden: !expanded
  }, /* @__PURE__ */ import_react.default.createElement(import_styles.ListBoxCard, {
    paddingY: 1,
    radius: 1,
    shadow: 2,
    tabIndex: -1,
    tone: "inherit"
  }, /* @__PURE__ */ import_react.default.createElement("ul", {
    "aria-multiselectable": false,
    id: listboxId,
    ref: listRef,
    role: "listbox"
  }, filteredOptions.map((option, optionIndex) => /* @__PURE__ */ import_react.default.createElement(import_autocompleteOption.AutocompleteOption, {
    id: `${id}-option-${optionIndex}`,
    key: optionIndex,
    onSelect: handleOptionSelect,
    selected: selectedIndex > -1 ? optionIndex === selectedIndex : currentOption === option,
    value: option.value
  }, (0, import_react.cloneElement)(renderOption(option), {disabled: loading, tabIndex: -1})))), /* @__PURE__ */ import_react.default.createElement(import_styles.LoadingCard, {
    padding: 3,
    style: {opacity: loading ? 0.5 : 0}
  }, /* @__PURE__ */ import_react.default.createElement(import_primitives.Spinner, null)))));
});
InnerAutocomplete.displayName = "Autocomplete";
const Autocomplete = InnerAutocomplete;
