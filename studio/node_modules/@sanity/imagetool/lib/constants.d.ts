export namespace DEFAULT_HOTSPOT {
    const x: number;
    const y: number;
    const height: number;
    const width: number;
}
export namespace DEFAULT_CROP {
    const top: number;
    const left: number;
    const right: number;
    const bottom: number;
}
//# sourceMappingURL=constants.d.ts.map